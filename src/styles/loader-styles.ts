import { css } from 'lit-element';

export default css`
  .protvista-loader {
    display: flex;
    justify-content: center;
    margin: 4rem 0;
  }
  .protvista-loader svg {
    height: 100px;
  }
`;
